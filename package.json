{
  "name": "mern-messages",
  "version": "1.0.0",
  "description": "MERN stack message board application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client-install": "cd client && npm install",
    "client-build": "cd client && npm run build",
    "build": "npm install && npm run client-install && npm run client-build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "concurrently": "^8.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}